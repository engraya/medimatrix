# Include the API prefix. HTTPS is required in production.
API_BASE_URL=http://localhost:4000/api/v1
# Frontend origin; also allowlist it in the backend CSRF configuration.
APP_ORIGIN=http://localhost:3000
